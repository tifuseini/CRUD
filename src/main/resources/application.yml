server:
  port: 9191

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    driver-class-name: org.postgresql.Driver
    hikari:
      username: postgres
      password: postgres
  jpa:
    generate-ddl: true


  application:
    name: test
    description: test

